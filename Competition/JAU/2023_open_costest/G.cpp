// problem: G번 - 수열 재배열
// id:
// time taken:
#include <bits/stdc++.h>
using namespace std;
int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;
    int *nums = (int *)malloc(sizeof(int) * n);
    for (int i = 0; i < n; i++)
        cin >> nums[i];
    

    return 0;
}