// problem: A1번 - SAVE the World (Small)
// id:
// time taken:
#include <bits/stdc++.h>
using namespace std;
int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, x, y;
    cin >> n;
    string s;
    vector<string> v;
    while (n--)
    {
        cin >> x >> y;
        s = "";
        int b = max(abs(x), abs(y));
        // if(x < 0 && y < 0)
    }

    return 0;
}