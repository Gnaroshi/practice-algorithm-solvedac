#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout << fixed;
    cout.precision(6);

    double r;
    cin >> r;

    cout << r * r * M_PI << '\n';
    cout << r * r * 2 << '\n';

    return 0;
}