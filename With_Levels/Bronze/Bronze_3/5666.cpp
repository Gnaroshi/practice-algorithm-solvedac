// problem: Hot Dogs
// id: 5666
// time taken:
#include <bits/stdc++.h>
using namespace std;
int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cout << fixed;
    cout.precision(2);
    double a, b;
    while (cin >> a >> b)
    {
        cout << a / b << '\n';
    }

    return 0;
}