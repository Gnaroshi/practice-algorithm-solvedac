// problem: Questionnaire
// id: 18964
// time taken:
#include <bits/stdc++.h>
using namespace std;
int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, m, k;
    cin >> n;
    int *p = (int *)malloc(sizeof(int) * n);
    for (int i = 0; i < n; i++)
        cin >> p[i];
    cout << 1 << ' ' << 0;

    return 0;
}
