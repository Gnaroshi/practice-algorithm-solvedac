// problem: Not A + B
// id: 23530
// time taken:
#include <bits/stdc++.h>
using namespace std;
int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int tc, a, b;
    cin >> tc;
    while (tc--)
    {
        cin >> a >> b;
        cout << 1 << '\n';
    }

    return 0;
}