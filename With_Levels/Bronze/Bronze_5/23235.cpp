#include <bits/stdc++.h>
using namespace std;
int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int cnt = 0;
    while (true)
    {
        int n;
        cin >> n;
        if (n == 0)
            break;
        for (int i = 0; i < n; i++)
        {
            int temp;
            cin >> temp;
        }
        cnt++;
        cout << "Case " << cnt << ": Sorting... done!\n";
    }
    return 0;
}