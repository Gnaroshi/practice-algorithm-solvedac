#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int s, t, d;
    cin >> s >> t >> d;
    cout << d / (s * 2) * t << '\n';

    return 0;
}