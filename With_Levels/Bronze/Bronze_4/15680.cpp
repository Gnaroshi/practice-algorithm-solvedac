#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    if (n)
        cout << "Leading the Way to the Future\n";
    else
        cout << "YONSEI\n";
    return 0;
}