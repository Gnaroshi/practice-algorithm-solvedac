// problem: 원의 분할
// id: 16478
// time taken:
#include <bits/stdc++.h>
using namespace std;
int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cout << fixed;
    cout.precision(7);

    double a, b, c;
    cin >> a >> b >> c;
    cout << a / b * c;

    return 0;
}