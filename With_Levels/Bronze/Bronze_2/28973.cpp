// problem: Разделение амулета
// id: 28973
// time taken:
#include <bits/stdc++.h>
using namespace std;
int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    double a, b, t;
    cin >> a >> b;
    cout << fixed;
    cout.precision(8);
    cout << (1 - sqrt(2) / 2) * a;

    return 0;
}