// problem: 패스트 푸드 상금
// id: 9329
// time taken:
#include <bits/stdc++.h>
using namespace std;
int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int tc; 
    cin >> tc;
    while (tc--)
    {
        int n, m;
        cin >> n >> m;
        int **prizes = (int **)malloc(sizeof(int *) * n);
        int *prize_amount = (int *)malloc(sizeof(int) * n);
        int *coach_stickers = (int *)malloc(sizeof(int) * m);
        for (int i = 0; i < n; i++)
        {
            int p;
            cin >> p;
            for (int j = 0; j < p; j++)
            {
            }
        }
    }

    return 0;
}